
/** Auth Steps
 *  1. create a new firebase project in console.firebase.google.com
 * 2.CLI (npm install firebase)
 * 3.create firebase.init.js file and include there const auth=getAuth(app) with import getAuth and export default auth
 * 4.Firebase setting > Authentication > enable Email and password auth
 * 5.Create Login,SignUp component , setup route
 * 6.attach form field handler and form submit handler
 * 7.CLI(npm install --save react-firebase-hooks)
 * 8.useCreateUserWithEmailAndPassword() from react firebase hooks
 */



/**
 * hosting steps
 * 1. CLI (npm install -g firebase-tools)
 * 2.CLI (firebase login)
 * 3.CLI (firebase init)
 * 4.CLI (firebase deploy)
 */





